services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: job_tracker
    ports:
      - '5432:5432'
    volumes:
      - job-tracker-db:/var/lib/postgresql/data

volumes:
  job-tracker-db:
